import 'package:flutter/material.dart';

class SearchSummaryPanel extends StatelessWidget {
  const SearchSummaryPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('検索・要約'),
    );
  }
}
