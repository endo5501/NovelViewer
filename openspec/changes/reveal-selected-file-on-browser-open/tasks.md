## 1. 現状を固定するテスト（実装前に失敗を確認）

- [x] 1.1 `file_browser_panel_test.dart` に、選択済みの状態でパネルをマウントしたとき遠くのファイル（200件中の150番目）が可視になることを検証するテストを追加する（`selectedFileProvider` を `_TestSelectedFileNotifier` で事前に上書きし、`SizedBox(height: 400)` に入れる）
- [x] 1.2 同ファイルに、その移動がアニメーションでないことを検証するテストを追加する（`pumpAndSettle` を使わず、`pump()` を 1〜2 回だけ回した時点でスクロール位置が目的値に達していることを確認する）
- [x] 1.3 選択が無い状態でマウントしたとき、スクロール位置が 0 のままであることを検証するテストを追加する
- [x] 1.4 選択ファイルが現在フォルダの一覧に含まれないとき、スクロール位置が 0 のままであることを検証するテストを追加する
- [x] 1.5 マウント後にフォルダを移動したとき、初回スクロールが再発火しないことを検証するテストを追加する（`currentDirectoryProvider` を切り替え、新しい一覧が先頭から表示されることを確認する）
- [x] 1.6 一覧が読み込み中の状態でマウントし、あとからデータが届いた場合にも選択ファイルが可視になることを検証するテストを追加する（`Completer` で解決を遅らせる）
- [x] 1.7 `Scaffold.drawer` にパネルを入れ、開く→閉じる→開く、の順で操作したあと選択ファイルが可視であることを検証するテストを追加する（実際の再マウント経路の回帰テスト）
- [x] 1.8 `fvm flutter test` を実行し、失敗したのは 1.1・1.2・1.5・1.6・1.7 の 5 テスト。いずれも「選択ファイルが見つからない」「スクロール位置が 0.0 のまま」という期待どおりの理由。1.3・1.4 は予想どおり現状でも通ったので現状維持の回帰テストとして残す。1.5 は予想に反して失敗した（フォルダ移動前の前提として初回スクロールを検証しているため）。全体は 3091 件通過・5 件失敗
- [x] 1.9 既存の「auto-scrolls to selected file」「reselecting the same file does not animate scroll」が引き続き通ることを確認する
- [ ] 1.10 テストのみをコミットする

## 2. 初回スクロールの実装

- [x] 2.1 `file_browser_panel.dart` の `_FileBrowserPanelState` に、マウントごとに一度だけ消費するフラグ（`_didInitialReveal`）を追加する
- [x] 2.2 `_scheduleScrollTo` にアニメーション有無の引数を追加し、`animateTo` と `jumpTo` を切り替えられるようにする。オフセット計算（フォルダ件数を足した flat index、中央寄せ、`maxScrollExtent` でのクランプ）は共有したまま変えない
- [x] 2.3 `_buildFileList` の `data` ブランチで `ListView` を返す直前に、フラグが未消費かつ選択ファイルがある場合に限り、アニメーション無しの初回スクロールを予約してフラグを立てる
- [x] 2.4 `contents.isEmpty` のブランチと `loading` / `error` のブランチではフラグを立てないことを確認する（決定 2）
- [x] 2.5 `initState` の `ref.listenManual` は選択変化用としてそのまま残し、アニメーション有りで呼ぶ
- [x] 2.6 フラグの意図（なぜ一度きりか、なぜ `ListView` を返すビルドで立てるのか）をコードコメントに残す。周囲の既存コメントと同じ粒度に合わせる
- [x] 2.7 `fvm flutter test` を実行し、1 で追加した全テストと既存テストが通ることを確認する（3096 件通過）。あわせて 1.5 のテストが実際に再発火を検出できることを、フラグの判定を一時的に外して失敗すること（0.0 のはずが 9400.5）で確認した

## 3. 実機確認

- [ ] 3.1 iPad 縦向き（narrow レイアウト）で、話数の多い小説の後半を開いた状態からファイルブラウザを開き、選択中のファイルが最初から見えることを確認する
- [ ] 3.2 その際にリストが動く様子が見えないこと（開いた時点で既にその位置にあること）を確認する
- [ ] 3.3 ファイルブラウザを開いたまま別フォルダへ移動し、勝手にスクロールしないことを確認する
- [ ] 3.4 iPad を縦横に回転させ、横向き（wide レイアウト）でも選択中のファイルが見えることを確認する
- [ ] 3.5 デスクトップで、ファイル選択時の既存のアニメーション付きスクロールが今までどおり動くことを確認する

## 4. 最終確認

- [ ] 4.1 code-reviewスキルを使用してコードレビューを実施
- [ ] 4.2 codexスキルを使用して現在開発中のコードレビューを実施
- [ ] 4.3 `fvm dart format .`でフォーマットを実行
- [ ] 4.4 `fvm flutter analyze`でリントを実行
- [ ] 4.5 `fvm flutter test`でテストを実行
