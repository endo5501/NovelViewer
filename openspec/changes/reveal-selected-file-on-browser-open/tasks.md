## 1. 現状を固定するテスト（実装前に失敗を確認）

- [x] 1.1 `file_browser_panel_test.dart` に、選択済みの状態でパネルをマウントしたとき遠くのファイル（200件中の150番目）が可視になることを検証するテストを追加する（`selectedFileProvider` を `_TestSelectedFileNotifier` で事前に上書きし、`SizedBox(height: 400)` に入れる）
- [x] 1.2 同ファイルに、リストを最初に合成するフレームが既に目的位置でレイアウトされていることを検証するテストを追加する（スクロールオフセットではなく `getRect` で描画矩形を比較する。オフセットはポストフレームのジャンプでも同じ値になり判別できない）
- [x] 1.3 選択が無い状態でマウントしたとき、スクロール位置が 0 のままであることを検証するテストを追加する
- [x] 1.4 選択ファイルが現在フォルダの一覧に含まれないとき、スクロール位置が 0 のままであることを検証するテストを追加する
- [x] 1.5 マウント後にフォルダを移動したとき、初回スクロールが再発火しないことを検証するテストを追加する（`currentDirectoryProvider` を切り替え、新しい一覧が先頭から表示されることを確認する）
- [x] 1.6 一覧が読み込み中の状態でマウントし、あとからデータが届いた場合にも選択ファイルが可視になることを検証するテストを追加する（`Completer` で解決を遅らせる）
- [x] 1.7 `Scaffold.drawer` にパネルを入れ、開く→閉じる→開く、の順で操作したあと選択ファイルが可視であることを検証するテストを追加する（実際の再マウント経路の回帰テスト）
- [x] 1.8 `fvm flutter test` を実行し、失敗したのは 1.1・1.2・1.5・1.6・1.7 の 5 テスト。いずれも「選択ファイルが見つからない」「スクロール位置が 0.0 のまま」という期待どおりの理由。1.3・1.4 は予想どおり現状でも通ったので現状維持の回帰テストとして残す。1.5 は予想に反して失敗した（フォルダ移動前の前提として初回スクロールを検証しているため）。全体は 3091 件通過・5 件失敗
- [x] 1.9 既存の「auto-scrolls to selected file」「reselecting the same file does not animate scroll」が引き続き通ることを確認する
- [x] 1.10 テストのみをコミットする（96890ae）

## 2. 初回スクロールの実装

- [x] 2.1 `_FileBrowserPanelState` の `_scrollController` を nullable にし、リストを組み立てるときに一度だけ生成する形へ変える
- [x] 2.2 flat index の算出（`_flatIndexOf`）と中央寄せ位置の計算（`_centeredOffset`）を関数へ切り出し、初回配置と選択変化のスクロールで共有する
- [x] 2.3 `_buildFileList` の `data` ブランチで `ListView` を `LayoutBuilder` で包み、`constraints.maxHeight` を使って `initialScrollOffset` 付きのコントローラを生成する。`maxScrollExtent` は `行数 * _kFileTileExtent - ビューポート高さ` から求める
- [x] 2.4 `contents.isEmpty` のブランチと `loading` / `error` のブランチではコントローラを作らないことを確認する（決定 2）
- [x] 2.5 `initState` の `ref.listenManual` は選択変化用としてそのまま残し、`animateTo` のまま変えない
- [x] 2.6 コントローラの生成を遅らせる理由（なぜ一度きりか、なぜ最初のレイアウトに間に合う必要があるのか）をコードコメントに残す。周囲の既存コメントと同じ粒度に合わせる
- [x] 2.7 `fvm flutter test` を実行し、1 で追加した全テストと既存テストが通ることを確認する（3096 件通過）。あわせて 1.5 のテストが実際に再発火を検出できることを、フラグの判定を一時的に外して失敗すること（0.0 のはずが 9400.5）で確認した

## 2b. レビュー指摘の反映

- [x] 2b.1 Codex レビュー（Major）: ポストフレームの `jumpTo` では先頭位置のフレームが 1 枚合成されることを確認し、`LayoutBuilder` と `initialScrollOffset` による方式へ実装を作り替える
- [x] 2b.2 Codex レビュー（Minor）: `find.text` は `ListView` のキャッシュ領域の行も拾うため、選択行がビューポート内にあり中央付近にあることを描画矩形で検証するテストへ強化する
- [x] 2b.3 Codex レビュー（Minor）: 選択変化のスクロールがアニメーションであることを固定するテストを追加する（途中経過を `pump(Duration)` で確認する）
- [x] 2b.4 Codex レビュー（Minor）: tasks 2.3 の文言を実装（未選択でも機会を消費する）に合わせて修正する
- [x] 2b.5 code-review 指摘: 起動時に空一覧が `isEmpty` ブランチへ来るというコメントの説明は誤り。`currentDirectory` が null の間は `_buildFileList` が呼ばれないため、コメントを事実に合わせて書き直す
- [x] 2b.6 1.2 のテストが旧実装（ポストフレームの `jumpTo`）で失敗し、新実装で通ることを確認する
- [x] 2b.7 `fvm flutter test` を実行し、全テストが通ることを確認する

## 3. 実機確認

- [x] 3.1 iPad 縦向き（narrow レイアウト）で、話数の多い小説の後半を開いた状態からファイルブラウザを開き、選択中のファイルが最初から見えることを確認する
- [x] 3.2 その際にリストが動く様子が見えないこと（開いた時点で既にその位置にあること）を確認する
- [x] 3.3 ファイルブラウザを開いたまま別フォルダへ移動し、勝手にスクロールしないことを確認する
- [x] 3.4 iPad を縦横に回転させ、横向き（wide レイアウト）でも選択中のファイルが見えることを確認する
- [x] 3.5 デスクトップで、ファイル選択時の既存のアニメーション付きスクロールが今までどおり動くことを確認する

## 4. 最終確認

- [x] 4.1 code-reviewスキルを使用してコードレビューを実施
- [x] 4.2 codexスキルを使用して現在開発中のコードレビューを実施
- [x] 4.3 `fvm dart format .`でフォーマットを実行
- [x] 4.4 `fvm flutter analyze`でリントを実行
- [x] 4.5 `fvm flutter test`でテストを実行
