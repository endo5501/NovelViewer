## 1. 準備

- [x] 1.1 `pubspec.yaml` に `app_links` を追加し、`fvm flutter pub get` を実行する
- [x] 1.2 `fvm flutter build macos` が通ることを確認し、依存追加がデスクトップビルドを壊していないことを確かめる

## 2. 要求のパースと検証（TDD）

- [x] 2.1 ダウンロード要求のパースと検証のテストを書く（正しい形式の受理、ホストが `download` 以外、`url` パラメータなし、`url` が空、`file://` などの非 http スキーム、パース不能な文字列）
- [x] 2.2 テストを実行して失敗することを確認する
- [x] 2.3 テストが正しいことを確認できた段階でコミットする
- [x] 2.4 ダウンロード要求を表す型と、`Uri` から要求を組み立てる純粋関数を実装してテストを通す

## 3. リンク入力源の抽象（TDD）

- [x] 3.1 リンク入力源のテストを書く（コールドスタートの初回リンクが一度だけ流れる、起動中の受信が流れる、初回リンクがストリームにも重複して届いた場合に二重処理しない）
- [x] 3.2 テストを実行して失敗することを確認する
- [x] 3.3 テストが正しいことを確認できた段階でコミットする
- [x] 3.4 `Stream<Uri>` を返す入力源の抽象と、fake 実装を用意してテストを通す
- [x] 3.5 `app_links` を包む本番実装を追加し、`getInitialLink()` と `uriLinkStream` の統合を入力源の内側に閉じる

## 4. 保留リクエストのプロバイダ（TDD）

- [x] 4.1 保留リクエストのプロバイダのテストを書く（有効な要求がセットされる、不正な要求は破棄されて状態が変わらない、同じ URL を連続で受信しても 2 回目が区別される）
- [x] 4.2 テストを実行して失敗することを確認する
- [x] 4.3 テストが正しいことを確認できた段階でコミットする
- [x] 4.4 `fileOpenRequestProvider` と同型の「要求」プロバイダを実装し、入力源と要求パーサを接続してテストを通す

## 5. 文言の追加

- [x] 5.1 拒否メッセージのキーを `lib/l10n/app_en.arb` / `app_ja.arb` / `app_zh.arb` の 3 ファイルに追加する
- [x] 5.2 `fvm flutter pub get` で `AppLocalizations` を再生成し、3 言語すべてにキーが存在することを確認する

## 6. DownloadDialog への反映と拒否（TDD）

- [x] 6.1 `DownloadDialog` のウィジェットテストを書く（初期 URL が入力欄に反映される、`idle` のときに受信すると URL 欄が差し替わる、`downloading` のときは URL 欄が変化せず拒否メッセージが表示される、`completed` のときも拒否メッセージが表示される、拒否時にダウンロードが開始されない）
- [x] 6.2 テストを実行して失敗することを確認する
- [x] 6.3 テストが正しいことを確認できた段階でコミットする
- [x] 6.4 `DownloadDialog` に初期 URL の受け入れを追加してテストを通す
- [x] 6.5 `DownloadDialog` が保留リクエストを監視し、`idle` なら URL 欄を差し替え、それ以外なら拒否メッセージを自身の中に表示する処理を実装する

## 7. HomeScreen からのダイアログ起動（TDD）

- [x] 7.1 `HomeScreen` のテストを書く（ダイアログ未表示のときに受信するとダイアログが URL 入りで開く、すでに開いているときは二重に開かない、受信だけではダウンロードが開始されない）
- [x] 7.2 テストを実行して失敗することを確認する
- [x] 7.3 テストが正しいことを確認できた段階でコミットする
- [x] 7.4 `HomeScreen` に保留リクエストの `ref.listen` を追加し、ダイアログ未表示のときだけ開く処理を実装する
- [x] 7.5 受信経路から `downloadProvider.startDownload` / `startCollectionDownload` を呼び出していないことをコード上で確認する

## 8. ネイティブへのスキーム登録

- [x] 8.1 `ios/Runner/Info.plist` に `CFBundleURLTypes` で `novelviewer` スキームを登録する
- [x] 8.2 `macos/Runner/Info.plist` に `CFBundleURLTypes` で `novelviewer` スキームを登録する
- [x] 8.3 Windows のインストーラ定義（`installer/novel_viewer.iss`）とアプリ起動処理にスキーム登録を追加していないことを確認する

## 9. 手動確認

- [ ] 9.1 macOS で、アプリ未起動の状態から `novelviewer://download?url=...` を開き、ダイアログが URL 入りで開くことを確認する
- [ ] 9.2 macOS で、アプリ起動中に同じリンクを開き、ダイアログが開くことを確認する
- [ ] 9.3 macOS で、ダウンロード中に同じリンクを開き、拒否メッセージが表示されダウンロードが継続することを確認する
- [ ] 9.4 iPad 実機で、ショートカットアプリから共有シート経由でリンクを送り、ダイアログが URL 入りで開くことを確認する
- [ ] 9.5 iPad 実機で、対応サイト以外の URL を送り、汎用 Web 記事としての取り込み選択肢が出ることを確認する
- [x] 9.6 上記の確認手順をチェックリストとして文書に残す

## 10. 文書

- [x] 10.1 README に `novelviewer://download?url=...` の形式と、iPad でショートカットから共有シートに出す手順を追記する
- [x] 10.2 README に Windows / Linux が本機能の対象外であること、およびその理由（Windows は単一インスタンス化が先に必要）を明記する

## 11. 最終確認

- [ ] 11.1 code-reviewスキルを使用してコードレビューを実施
- [ ] 11.2 codexスキルを使用して現在開発中のコードレビューを実施
- [x] 11.3 `fvm dart format .`でフォーマットを実行
- [x] 11.4 `fvm flutter analyze`でリントを実行
- [x] 11.5 `fvm flutter test`でテストを実行
