## 1. 共有ルビ除去ヘルパの抽出

D7 の純粋な移動。後続の変更の差分を読みやすくするため先に単独で行う。

- [x] 1.1 `lib/shared/` にルビ注釈除去ヘルパを新設し、`<rt>…</rt>` / `<rp>…</rp>` / `<ruby>` / `</ruby>` を除去して base を残すことを確認する単体テストを追加する（テストのみ。実行して失敗することを確認する）
- [x] 1.2 ヘルパを実装し、1.1 のテストが通ることを `fvm flutter test` で確認する
- [x] 1.3 `mark_matcher.dart` と `first_line_containing.dart` の重複した除去パターンを共有ヘルパの呼び出しに置き換え、両ファイルの既存テストが**1件も変更せずに**通ることを `fvm flutter test test/features/llm_summary/` で確認する

## 2. 拡張文脈検索のルビ対応

specs/text-search を参照。判定のみをルビ除去後のテキストに対して行い、返す文字列は生のまま（D2）。

- [x] 2.1 `searchWithContext` に対するテストを追加する（テストのみ。実行して失敗することを確認する）
  - ルビ base と素テキストにまたがる語（`<ruby>紅蓮<rt>ぐれん</rt></ruby>の剣` を `紅蓮の剣` で）が採用される
  - 2つのルビグループにまたがる語が採用される
  - ルビ base の内側に収まる語が採用される（修正前から通る回帰ガード）
  - ルビを含まない語が採用される（回帰ガード）
  - 読み（`ぐれん`）では採用されない
  - 返される `contextText` / `extendedContext` がルビ注釈を含む生の本文のままである
- [x] 2.2 `TextSearchService.search`（Ctrl+F 経路）がルビに分断された語を採用**しない**ことを明示するテストを追加し、実行して**この時点で通る**ことを確認する（D3 の不変を固定するガードであり、2.3 の実装後も通り続けなければならない）
- [x] 2.3 `searchWithContext` の一致判定を共有ヘルパでルビ除去したテキストに対して行うよう実装し、2.1 と 2.2 のテストがすべて通ることを確認する
- [x] 2.4 `search` と `searchWithContext` で一致判定が分かれる理由（UI検索はハイライトがセグメント単位のため判定だけを変えると不整合になる／LLM経路はハイライトしないので判定だけで足りる）をコードのコメントとして残し、両メソッドの近接した本体を読む者が不具合と誤認しないことを確認する

## 3. 簡易解析スコープと初出話数の解決

specs/llm-summary-context-menu-trigger を参照。D1・D4・D5。

- [x] 3.1 初出話数の解決に対するテストを追加する（テストのみ。実行して失敗することを確認する）
  - 語が複数ファイルに出るとき、有効話数が最小のファイルの話数に解決される
  - 数値接頭辞の無いファイルでは辞書順ランクが使われる
  - 同じ話数を共有する複数ファイル（`005_a.txt` / `005_b.txt`）がある場合、その話数に解決される
  - 語を含むファイルが1つも無いとき、現在閲覧中のファイルの話数に解決される
  - 解決結果として `sourceFileName` が初出ファイルになる
  - 現在ページでのみルビに分断されて出現し後のページに素で出る語について、解決される話数が現在ページの話数になる（`specs/llm-summary-context-menu-trigger` の不変条件。2.3 に依存する）
- [x] 3.2 `AnalysisScope` に簡易スコープを追加し、`runWithScope` が `searchWithContext` の結果から最小話数と初出ファイル名を解決して既存の `run()` に渡すよう実装し、3.1 のテストが通ることを確認する
- [x] 3.3 語を含むファイルが無い場合に、スナップショットが保存されず既存の「情報を抽出できなかった」失敗通知が語名付きで提示されることをテストで確認する
- [x] 3.4 簡易解析で解決した話数のファイル群のみが抽出対象になり、後続ファイルが抽出されないことをテストで確認する（`LlmSummaryService` を変更していないことの確認も兼ねる）

## 4. コンテキストメニュー項目

specs/llm-summary-context-menu-trigger を参照。D8。

- [x] 4.1 `lib/l10n/app_ja.arb` に「解析開始(簡易)」のキーを追加し、`app_en.arb` / `app_zh.arb` にも対応する訳を追加する。`fvm flutter gen-l10n` で生成物が更新されることを確認する
- [x] 4.2 両メニュービルダーに対するテストを追加する（テストのみ。実行して失敗することを確認する）
  - 選択があるとき3項目が「簡易 → ネタバレなし → ネタバレあり」の順で並ぶ（縦書き・横書きの両ビルダー）
  - 選択が空のとき3項目すべてが出ない
  - 解析ラベルが与えられないとき3項目すべてが出ず、辞書項目は出る（グループとして扱われること）
  - 簡易項目のディスパッチが選択語と簡易スコープを渡す
  - 解析ハンドラが与えられないとき3項目すべてが no-op になる
- [ ] 4.3 `vertical_context_menu.dart` に項目と `dispatchVerticalContextAction` のケースを追加し、`dictionary_context_menu.dart` のグループ条件に新ラベルを加えて実装し、4.2 のテストと両ファイルの既存テストが通ることを確認する
- [ ] 4.4 `text_content_renderer.dart` の呼び出し側で新ラベルを渡し、LLM 非対応プラットフォームでは3項目すべてが withheld されることを既存の capability ゲートのテストで確認する

## 5. ホバーポップアップのドロップダウン項目

specs/llm-summary-hover-popup を参照。D9。

- [ ] 5.1 再解析ドロップダウンに対するテストを追加する（テストのみ。実行して失敗することを確認する）
  - 3項目が「簡易解析 → 現在ページまで → 全話まで」の順で並ぶ
  - 簡易解析項目に話数も `(上書き)` サフィックスも付かない（スナップショットが存在する場合も含む）
  - ドロップダウンを開く操作でフォルダの検索が行われない
  - 簡易解析項目の選択が簡易スコープで解析を開始し、確認ダイアログを出さない
  - 既存2項目の話数表示と `(上書き)` サフィックスの挙動が変わらない（回帰ガード）
- [ ] 5.2 `hover_popup_widget.dart` の `_ReanalyzeMenuButton` に項目を追加して実装し、5.1 のテストと既存のポップアップテストが通ることを確認する

## 6. 最終確認

- [ ] 6.1 code-reviewスキルを使用してコードレビューを実施
- [ ] 6.2 codexスキルを使用して現在開発中のコードレビューを実施
- [ ] 6.3 `fvm dart format .`でフォーマットを実行
- [ ] 6.4 `fvm flutter analyze`でリントを実行
- [ ] 6.5 `fvm flutter test`でテストを実行
- [ ] 6.6 実機で確認する: 頻出語を簡易解析して待ち時間が従来のネタバレなし解析より大幅に短いこと、履歴パネルの当該スナップショットからのジャンプが初出ページに着地すること
