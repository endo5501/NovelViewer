## 1. ポップアップ配置の画面内クランプ

純関数で独立しており、他のどの作業にも依存しない。ここから始めて土台を固める。

- [x] 1.1 `test/features/llm_summary/presentation/hover_popup_anchor_test.dart` に横書きの端処理のテストを追加する。右端でのめくり返し、下端でのめくり返し、どちらも収まらない場合の原点クランプ、そして余裕がある通常の場合に従来どおり右下 16px であること、の四つ
- [x] 1.2 テストを実行し、追加した分だけが失敗することを確認する
- [x] 1.3 `lib/features/llm_summary/presentation/hover_popup_anchor.dart` の横書き分岐に、縦書きと同じめくり返しとクランプを適用する
- [x] 1.4 テストが通ることを確認し、縦書き側の既存テストが変わらず通ることも確認する

## 2. マーク一覧の共有化

横書きでタップ位置から語句を引くために、`buildRubyTextSpans` の内部にあるマーク算出を呼び出し側からも使えるようにする。

- [x] 2.1 平文テキストの連結と `findMarks` の呼び出しを行う関数のテストを書く。`buildRubyTextSpans` が組み立てる `TextSpan` のマーク範囲と、この関数が返す範囲が一致することを、ルビをまたぐ語句を含む入力で確認する
- [x] 2.2 テストを実行し、失敗することを確認する
- [x] 2.3 `lib/features/text_viewer/presentation/ruby_text_builder.dart` から該当処理を公開関数として切り出し、`buildRubyTextSpans` 自身もその関数を使うように書き換える
- [x] 2.4 テストが通ることと、既存のルビ・マーク関連テストが変わらず通ることを確認する

## 3. 縦書きのタップ分岐

- [x] 3.1 `vertical_text_page` に対するウィジェットテストを追加する。指のタップがマーク上に落ちたときにマークのコールバックが呼ばれること、選択範囲の内側のタップではコンテキストメニューが優先されてマークのコールバックが呼ばれないこと、どちらにも当たらないタップで従来どおり選択がクリアされること、マウスのクリックではマークのコールバックが呼ばれないこと
- [x] 3.2 桁の隙間へのタップが、隣接するマーク文字として解決されることのテストを追加する
- [x] 3.3 テストを実行し、追加した分だけが失敗することを確認する
- [x] 3.4 `lib/features/text_viewer/presentation/vertical_text_page.dart` の `_onTapUp` に、選択判定とクリアの間へマーク判定を挟む。ヒットテストの結果は既存の呼び出しから使い回す
- [x] 3.5 `lib/features/text_viewer/presentation/vertical_text_viewer.dart` に、マークのタップをページから上位へ運ぶコールバックを通す
- [x] 3.6 `lib/features/text_viewer/presentation/widgets/text_content_renderer.dart` で受け取り、`hoverPopupProvider` の `show` を呼ぶ
- [x] 3.7 テストが通ることと、`touch-context-menu-trigger` の既存テストが変わらず通ることを確認する

## 4. 横書きのタップ分岐

design.md の決定3で挙げたタイミングの不確かさがあるため、実装より先に検証用のテストを置く。

- [x] 4.1 `text_content_renderer` に対するウィジェットテストを追加する。指のタップがマーク上に落ちたときにポップアップの状態が可視になること、マークのない場所のタップでは可視にならないこと、マウスのクリックでは可視にならないこと
- [x] 4.2 語端スナップの扱いを確かめるテストを追加する。報告されるオフセットがマーク範囲の終端に一致する場合でも、そのマークが引けること
- [x] 4.3 同じ語句を閉じてからもう一度タップしたときに再び開くことのテストを追加する。`onSelectionChanged` が二度目に呼ばれない場合を模す
- [x] 4.4 テストを実行し、追加した分だけが失敗することを確認する
- [x] 4.5 `text_content_renderer.dart` に、`SelectableText` を包む `Listener` と、`onSelectionChanged` が報告した表示オフセットの保持、ポインタアップ後に一度だけ走る解決処理を実装する
- [x] 4.6 解決処理でマーク一覧 (2 で切り出した関数の結果) を引き、見つかれば `show`、見つからなければ `hide` を呼ぶ。マーク一覧は既存の `_cachedTextSpan` と同じ条件でキャッシュする
- [x] 4.7 テストが通ることを確認する。マイクロタスクの時点で解決できない場合は、design.md の記述どおりポストフレームコールバックへ切り替える
- [x] 4.8 横書きの既存テスト、とくに TTS の再生開始位置に関わる選択オフセットのテストが変わらず通ることを確認する

## 5. タッチによる閉じる操作

- [x] 5.1 `hover_popup_host` に対するウィジェットテストを追加する。ポップアップの外側のタッチで閉じること、ポップアップの内側のタッチでは閉じないこと、マウスのポインタダウンでは閉じないこと、子メニューが開いている間は閉じないこと
- [x] 5.2 透過リスナーが下のウィジェットを塞がないことのテストを追加する。ポップアップを開いた状態で背後のボタンをタップし、そのボタンが反応することを確認する
- [x] 5.3 テストを実行し、追加した分だけが失敗することを確認する
- [x] 5.4 `lib/features/llm_summary/presentation/hover_popup_host.dart` に、ポップアップより下へ挿入する画面全体の透過リスナーのオーバーレイを実装する。ポップアップ本体には `GlobalKey` を持たせ、矩形が取れない間は閉じない側へ倒す
- [x] 5.5 子メニューが開いているかどうかの判定は、`HoverPopupNotifier` が持つ既存の状態を読み出せる形にする
- [x] 5.6 テストが通ることと、既存のホバー経路のテストが一つも変わらないことを確認する

## 6. 横書きのスクロールで閉じる

- [x] 6.1 読者が動かしたスクロールでポップアップが閉じ、音声追従の自動スクロールでは閉じないことのテストを追加する
- [x] 6.2 テストを実行し、失敗することを確認する
- [x] 6.3 `text_content_renderer.dart` の `NotificationListener<ScrollNotification>` から `hide` を呼ぶ。既存の `dragDetails` と `_isTtsScrolling` による区別をそのまま使う
- [x] 6.4 テストが通ることを確認する

## 7. 実機とシミュレータでの確認

- [x] 7.1 iPad シミュレータで、横書き・縦書きの双方で解析済み語句をタップし、要約が表示されることを確認する
- [x] 7.2 ポップアップの外側をタップして閉じること、ポップアップ内のスナップショット送りと再解析ドロップダウンが指で操作できることを確認する
- [x] 7.3 縦持ちで画面右寄りの語句をタップし、ポップアップが画面内に収まることを確認する
- [x] 7.4 縦書きで範囲選択したあと、その内側をタップしてコンテキストメニューが出ること、マークの上をタップして要約が出ること、どちらでもない場所をタップして選択が消えることを確認する
- [x] 7.5 デスクトップ (macOS) でホバーの挙動が変わっていないことを確認する。マークへの出入り、猶予時間を使ったポップアップへの移動、再解析ドロップダウンの操作

## 8. 最終確認

- [ ] 8.1 code-reviewスキルを使用してコードレビューを実施
- [ ] 8.2 codexスキルを使用して現在開発中のコードレビューを実施
- [ ] 8.3 `fvm dart format .`でフォーマットを実行
- [ ] 8.4 `fvm flutter analyze`でリントを実行
- [ ] 8.5 `fvm flutter test`でテストを実行
