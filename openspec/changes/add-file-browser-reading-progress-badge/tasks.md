## 1. リポジトリ: 進捗の一括取得（TDD）

- [x] 1.1 `ReadingProgressRepository` の一括取得メソッド（`findAll` 相当、全 `reading_progress` 行を `novel_id` / `file_name` で返す）のテストを先に書く（複数行・空・例外時 WARNING ログの3シナリオ）
- [x] 1.2 テストが失敗することを確認する
- [x] 1.3 一括取得メソッドを実装してテストをパスさせる（1クエリで取得。スキーマ変更なし）

## 2. ドメイン: 進捗バッジデータの算出（TDD）

- [x] 2.1 `file_name` 先頭数字のパース（`003_...txt` → 3、非数字・先頭数字なしは 0）と、`read`/`total` 算出ロジックのテストを書く
- [x] 2.2 テストが失敗することを確認する
- [x] 2.3 算出ロジック（純粋関数 or 小さなモデル）を実装してテストをパスさせる

## 3. プロバイダ: 表示中小説の進捗結合（TDD）

- [x] 3.1 `allNovelsProvider`（`folderName → episode_count`）と一括取得（`novel_id → file_name`）を結合し、`folderName → (read, total)` を返すプロバイダのテストを書く（読みかけ／未読は `0 / N`／手動フォルダは対象外、の各ケース）
- [x] 3.2 テストが失敗することを確認する
- [x] 3.3 バッジ表示用プロバイダを `file_browser_providers.dart` に実装してテストをパスさせる（フォルダDB非アクセス・FS走査なし）

## 4. UI: 小説フォルダタイルへの進捗バー表示

- [x] 4.1 `_buildDirectoryTile` の登録済み小説タイルに、フォルダ名の下（`subtitle`）へ進捗バー＋`read / total` の数値を表示する（手動フォルダ・整理フォルダには出さない）
- [x] 4.2 2行レイアウトに合わせて `_kFileTileExtent` を引き上げ、`_scheduleScrollTo` のスクロール自動追従が壊れていないことを確認する（既存のオフスクリーン自動スクロールテストがパス）
- [x] 4.3 未読（`reading_progress` 行なし）が `0 / N`・0% バーで表示されることを確認する
- [x] 4.4 ウィジェットテストで進捗バーの表示有無・数値を検証する

## 5. 最終確認

- [ ] 5.1 code-reviewスキルを使用してコードレビューを実施
- [ ] 5.2 codexスキルを使用して現在開発中のコードレビューを実施
- [ ] 5.3 `fvm flutter analyze`でリントを実行
- [ ] 5.4 `fvm flutter test`でテストを実行
