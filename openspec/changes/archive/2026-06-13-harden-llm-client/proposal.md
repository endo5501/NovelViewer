## Why

LLMクライアント（OpenAI互換 / Ollama）のレスポンス処理には、ユーザに見えない形でデータを破損させたりクラッシュさせたりする欠陥が複数残っている（TECH_DEBT_AUDIT.md の F113 / F132 / F163）。

- **文字化けがキャッシュに焼き付く（F113）**: `response.body` は `Content-Type` に charset 指定が無いと latin1 にフォールバックする。実OpenAI互換エンドポイントは裸の `application/json` を返すため、日本語要約が文字化けし、それがそのまま `fact_cache` / 要約として永続化される。
- **生の例外が設定UIに漏れる（F113）**: `choices[0]`（空配列で `RangeError`）、`json['models'] as List`（型不一致で `TypeError`）など、サーバ応答の形状を信頼したキャストが診断不能な生例外として設定画面に届く。
- **壊れた要約が永続化される（F132）**: パイプラインの JSON 抽出は、応答が妥当なJSONで対象キーを持つが値が String でない（例: `{"summary": null}`）場合、`as String` の `CastError` が catch に落ち、**生JSON文字列を要約として返してキャッシュに保存**する。
- **HTTPクライアントのリーク（F163）**: LLMクライアントが `httpClientProvider` を注入されず、内部で未close の `http.Client()` を設定変更のたびに生成する。正規の共有クライアントが他機能では使われているのに、LLM系だけ DI 規約から外れている。

これらは F143 が指摘する通りテストが存在しないネットワークパーサ上にあり、現挙動の固定（失敗系テスト）から TDD で着手するのに適している。

## What Changes

- LLMクライアント2本（`OpenAiCompatibleClient` / `OllamaClient`）の全レスポンス読み取りを `utf8.decode(response.bodyBytes)` に統一し、charset 非依存で日本語を正しく復号する。
- サーバ応答の形状を検証する: `choices` 空配列、`message` 欠落、`models` 非配列、必須フィールド欠落・型不一致を**型付き例外**（新設の `LlmResponseFormatException` 系）に変換し、生の `RangeError` / `TypeError` を呼び出し側／UIへ漏らさない。
- **BREAKING（内部API）**: 要約パイプラインの JSON 抽出で、対象キーの値が String でない場合に生JSON文字列を要約として永続化する挙動を撤廃する。値が不正なときは「壊れた応答」として扱い（要約として保存しない）、デコード失敗時の既存フォールバック契約を「キーは在るが型が不正」のケースまで拡張する。
- LLMクライアントを生成する provider が共有 `httpClientProvider` を注入するようにし、内部での `http.Client()` 生成（未close）を撤廃して DI 規約を必須注入に統一する。
- 上記すべてについて、現挙動を固定する失敗系テストを先に追加する（TDD）。

## Capabilities

### New Capabilities
- `llm-client-robustness`: LLMクライアント（OpenAI互換 / Ollama）が、HTTPレスポンスを文字コードに依存せず復号し、応答の形状を検証して型付き例外に変換し、HTTPクライアントを外部注入で受け取る、という横断的な堅牢性要件を定める。

### Modified Capabilities
- `llm-summary-pipeline`: 既存の「JSON decode failure observability」要件を変更し、JSONとしては妥当だが対象キーの値が String でない応答を「壊れた応答」として扱う（生JSON文字列を要約値として採用しない）挙動を追加する。

## Impact

- コード:
  - `lib/features/llm_summary/data/openai_compatible_client.dart`（charset / `choices`・`message`・`content` の形状検証）
  - `lib/features/llm_summary/data/ollama_client.dart`（charset / `models`・`name`・`response` の形状検証）
  - `lib/features/llm_summary/data/llm_summary_pipeline.dart:169-187`（`_parseJsonResponse` の型検証）
  - `lib/features/llm_summary/providers/llm_summary_providers.dart:22-36`（`httpClientProvider` 注入）
  - 例外型の新設（`lib/features/llm_summary/data/` 配下）
- 依存: 新規パッケージ追加なし（`dart:convert` の `utf8` と既存 `httpClientProvider` のみ）。
- テスト: LLMクライアント2本・パイプライン抽出・provider 注入の新規テスト（現状カバレッジゼロ → F143 を一部解消）。
- 後方互換: 公開UI挙動は不変（むしろ文字化け・クラッシュが解消）。内部的に「不正値を要約として保存しない」点のみ挙動変更。
