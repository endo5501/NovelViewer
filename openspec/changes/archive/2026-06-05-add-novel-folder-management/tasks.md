## 1. FileSystemService の階層操作 API（TDD）

- [x] 1.1 `createDirectory` のテストを作成（有効名で作成成功、同名衝突で例外、無効文字で例外）し、失敗を確認する
- [x] 1.2 `createDirectory(parentPath, name)` を実装してテストを通す
- [x] 1.3 `renameDirectory` のテストを作成（同階層で改名成功、同名衝突で例外）し、失敗を確認する
- [x] 1.4 `renameDirectory(path, newName)` を実装してテストを通す
- [x] 1.5 `moveDirectory` のテストを作成（移動成功で葉名不変・新パス返却、移動先同名衝突で例外、自分自身/子孫への移動で例外）し、失敗を確認する
- [x] 1.6 `moveDirectory(srcPath, destParentPath)` を `Directory.rename` ベースで実装してテストを通す
- [x] 1.7 `deleteEmptyDirectory` のテストを作成（空フォルダ削除成功、空でない場合は例外）し、失敗を確認する
- [x] 1.8 `deleteEmptyDirectory(path)` を実装してテストを通す
- [x] 1.9 無効文字チェック／名前正規化をダウンロード側 `safeName` と整合させる（再利用または共通化）

## 2. 小説／整理フォルダ判別とプロバイダの階層対応（TDD）

- [x] 2.1 「basename が `folder_name` 集合に含まれるか」で小説判定する純粋関数のテストを作成し、失敗を確認する
- [x] 2.2 判別ヘルパーを実装してテストを通す
- [x] 2.3 `directoryContentsProvider` のテストを更新／追加（任意の深さで小説フォルダにタイトル変換が効く、整理フォルダはフォルダ名のまま）し、失敗を確認する
- [x] 2.4 `directoryContentsProvider` を `isLibraryRoot` 依存から判別軸ベースへ変更してテストを通す
- [x] 2.5 `selectedNovelTitleProvider` のテストを追加（深さ1で従来どおり、入れ子の小説でも basename ベースで正しく解決）し、失敗を確認する
- [x] 2.6 `selectedNovelTitleProvider` を basename ベース判定へ変更してテストを通す

## 3. ↑ナビゲーションのライブラリ境界修正（TDD）

- [x] 3.1 `getParentDirectory` の境界テストを作成（`currentDir == libraryPath` のとき `null`、配下では親を返す、Windows パス対応）し、失敗を確認する
- [x] 3.2 `getParentDirectory` を `libraryPath` 境界対応に変更してテストを通す
- [x] 3.3 ライブラリルートで↑ボタンが無効化（または無操作）になるよう `_buildToolbar` / `_navigateToParent` を修正する

## 4. 整理フォルダの作成・リネーム UI（TDD）

- [x] 4.1 新規フォルダ作成ダイアログ＋実行フローのウィジェットテストを作成し、失敗を確認する
- [x] 4.2 ツールバー（または空白部右クリック）に「新規フォルダ作成」操作を追加し、`createDirectory` を呼んで一覧を更新する
- [x] 4.3 右クリックメニューを判別軸で分岐（小説＝タイトル変更、整理フォルダ＝実フォルダ名リネーム）するテストを作成し、失敗を確認する
- [x] 4.4 `_buildDirectoryTile` / `_showContextMenu` を判別軸ベースに変更し、整理フォルダのリネーム（`renameDirectory`）を実装してテストを通す
- [x] 4.5 小説フォルダのリネームが従来どおり表示タイトルのみ更新であることを回帰テストで担保する

## 5. 移動機能と移動先ツリー選択ダイアログ（TDD）

- [x] 5.1 移動先ツリーダイアログのテストを作成（ライブラリ内の整理フォルダ階層を表示、ライブラリ外非表示、移動元自身/子孫は選択不可、小説フォルダ内部は移動先に出さない）し、失敗を確認する
- [x] 5.2 移動先ツリー選択ダイアログウィジェットを実装してテストを通す
- [x] 5.3 右クリックメニューに「移動」を追加（順序：更新・タイトル変更・移動・削除）し、選択結果で `moveDirectory` を呼ぶフローのテストを作成し、失敗を確認する
- [x] 5.4 移動フローを実装してテストを通す（成功で一覧更新、同名衝突や子孫移動でエラー表示）

## 6. 整理フォルダの削除（TDD）

- [x] 6.1 整理フォルダ削除フローのテストを作成（空なら削除、空でなければ拒否＋メッセージ）し、失敗を確認する
- [x] 6.2 整理フォルダの右クリック「削除」を `deleteEmptyDirectory` で実装してテストを通す

## 7. 移動・削除時の DB ハンドル整合（TDD）

- [x] 7.1 移動・削除前に対象および子孫の per-folder DB ハンドル（TTS音声DB・TTS辞書DB・エピソードキャッシュDB）を invalidate する処理のテストを作成し、失敗を確認する
- [x] 7.2 ハンドル解放処理を実装してテストを通す
- [x] 7.3 現在開いているフォルダ配下を移動した場合に `currentDirectory` を新パスへ追従させる（または安全に拒否する）処理を実装し、テストで担保する
- [x] 7.4 移動後に TTS音声データ・読書進捗・要約キャッシュが維持されることを統合的に確認するテストを追加する

## 8. i18n 文言追加

- [x] 8.1 新規フォルダ作成・移動・整理フォルダ削除・各種エラー（同名衝突、空でないフォルダ削除、自分自身/子孫への移動、無効文字）の文言を `app_localizations`（ja/en/zh）へ追加する
- [x] 8.2 右クリックメニューの「移動」ラベルを追加する

## 9. 最終確認

- [x] 9.1 code-reviewスキルを使用してコードレビューを実施
- [x] 9.2 codexスキルを使用して現在開発中のコードレビューを実施
- [x] 9.3 `fvm flutter analyze`でリントを実行
- [x] 9.4 `fvm flutter test`でテストを実行
