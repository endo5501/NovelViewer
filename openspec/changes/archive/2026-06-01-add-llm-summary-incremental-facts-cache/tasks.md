## 1. データベースマイグレーション: fact_cache テーブル

- [x] 1.1 新 DB バージョンが `(folder_name, word, file_name, facts, content_hash, prompt_version, updated_at)` カラムと `(folder_name, word, file_name)` のユニークキーを持つ `fact_cache` テーブルを作成することを検証する失敗するマイグレーションテストを書く
- [x] 1.2 `fact_cache` テーブルとユニークインデックスを作成する追加マイグレーション（DB バージョンをバンプ）を実装し、テストが通ることを確認する
- [x] 1.3 マイグレーションが前方のみで `word_summaries` に手を加えないことを確認する（既存マイグレーションテストが引き続き通る）

## 2. FactCacheRepository

- [x] 2.1 `FactCacheRepository` の失敗するテストを書く: 行の upsert、`(folder, word, file)` での参照、`(folder, word)` の一括読み出し、upsert がインプレース置換され重複しないこと
- [x] 2.2 無効化の失敗するテストを書く: `invalidateWord(folder, word)` が `content_hash` を空文字列のセントネルに設定し、セントネル行が妥当性チェックで無効と判定されること
- [x] 2.3 カスケード削除の失敗するテストを書く: `deleteAllForWord` と `deleteByFolderName` が該当する `fact_cache` 行を削除すること
- [x] 2.4 `FactCacheRepository`（upsert・読み出し・invalidateWord・カスケード削除ヘルパー）と `promptVersion` 定数を実装し、テストが通ることを確認する

## 3. コンテンツハッシュ＋妥当性チェック

- [x] 3.1 ファイル全内容に対するコンテンツハッシュヘルパー（安定・決定的）と妥当性述語（行が存在し AND content_hash 一致 AND prompt_version 一致のときのみ有効、セントネルは常に無効）の失敗するテストを書く
- [x] 3.2 ハッシュヘルパーと妥当性述語を実装し、テストが通ることを確認する

## 4. パイプラインのファイル単位 Stage-1 抽出

- [x] 4.1 Stage-1 抽出がソースファイルでグループ化されることを検証する失敗するテストを書く: 異なるファイルのエントリが同一チャンクに詰められない、小さいファイルは 1 チャンク、12000 字のファイルはそのファイルのファクトに統合される ~3 個の内部チャンクになる、サイズ超過の単一エントリは単独チャンクになる（`llm-summary-pipeline` MODIFIED 仕様に準拠）
- [x] 4.2 `LlmSummaryPipeline` をリファクタし、Stage-1 のファイル単位抽出と集約＋最終要約ステージを個別に呼び出せるようにする。集約・最終の挙動は変更しない
- [x] 4.3 既存のパイプラインテスト（集約・再帰・進捗イベント・JSON パース・リソース解放）が引き続き通ることを確認する

## 5. LlmSummaryService のキャッシュ併用オーケストレーション

- [x] 5.1 失敗するテストを書く: ファイル 1〜7 のうち 1〜5 が有効なキャッシュ行を持ち 6〜7 がミスのとき、サービスが 1〜5 のキャッシュ済みファクトを再利用し、6〜7 にのみ Stage-1 を発行し、組み立てたファクトが集約・最終に渡ること
- [x] 5.2 コールドキャッシュ実行が対象内の全ファイルを抽出し、キャッシュ導入前のコードパスと同一の要約を生成することを検証する失敗するテストを書く
- [x] 5.3 変更されたファイル（ハッシュ不一致）と prompt_version 不一致が、それぞれ影響を受けたファイルだけの再抽出を強制することを検証する失敗するテストを書く
- [x] 5.4 抽出に成功したファイルが現在のコンテンツハッシュと prompt version とともに `fact_cache` に書き戻されることを検証する失敗するテストを書く
- [x] 5.5 サービスのオーケストレーションを実装する: 絞り込み済み検索結果をファイルごとにグループ化 → ファイルごとにヒット or 抽出 → ファクトを組み立て → 集約＋要約 → キャッシュ行とスナップショットを永続化。テストが通ることを確認する

## 6. 再解析の強制無効化

- [x] 6.1 失敗するテスト（runner/service レベル）を書く: 既存 `covered_up_to_episode` での再解析が、実行前にその語のキャッシュ全体（`(folder, word)` の全行）をセントネル無効化し、ファイルがキャッシュ再利用ではなく再抽出されること
- [x] 6.2 再解析トリガが解析前に `invalidateWord` を呼ぶよう配線する（サービスの上書き検知で実現）。テストが通り、スナップショットがフレッシュな要約で上書きされることを確認する

## 7. 進捗＋UX の整合

- [x] 7.1 一部ファイルがキャッシュヒットのとき、解析モーダルの進捗イベントが正しく発火することを確認する（カウンタが実際に抽出したファイルだけを反映する）。必要に応じてテストを追加・調整する
- [x] 7.2 hover popup・履歴 UI・マーク描画が影響を受けないことを確認する（スナップショットスキーマ無変更）。既存テストを実行する

## 8. 最終確認

- [x] 8.1 code-reviewスキルを使用してコードレビューを実施
- [x] 8.2 codexスキルを使用して現在開発中のコードレビューを実施
- [x] 8.3 `fvm flutter analyze`でリントを実行
- [x] 8.4 `fvm flutter test`でテストを実行
