## 1. 保存先候補の列挙ロジック（TDD）

- [x] 1.1 ライブラリルート配下の整理用フォルダ（非小説フォルダ）を再帰列挙し、小説フォルダのサブツリーを枝刈りするロジックのテストを作成する（登録済み folder_name 集合を入力、小説フォルダとその配下を除外、ルートを先頭候補に含む）
- [x] 1.2 テストを実行して失敗を確認する
- [x] 1.3 列挙ロジックを実装してテストをパスさせる（`file_system_service` / 専用ドメイン関数いずれか。`isNovelFolder` と `allNovelsProvider` の folder_name を利用）
- [x] 1.4 候補を提供する Riverpod provider のテストと実装を追加する

## 2. ダウンロードダイアログの保存先選択UI（TDD）

- [x] 2.1 ダイアログに保存先選択UI（既定=ライブラリルート、候補=整理用フォルダの相対パス表示）を追加するウィジェットテストを作成する
- [x] 2.2 テストを実行して失敗を確認する
- [x] 2.3 選択UIを実装し、選択値を `startDownload(outputPath:)` に渡すよう `download_dialog.dart` を修正する（未選択時はルート＝現状互換）
- [x] 2.4 候補が空（整理用フォルダなし）の場合にルートのみ表示されることを検証する

## 3. ローカライズ

- [x] 3.1 保存先選択UIの新規文字列を en/ja/zh の `.arb` に追加する（ラベル「保存先フォルダ」、ルート選択肢の表示名 等）
- [x] 3.2 `gen-l10n` で未翻訳警告が出ないことを確認する

## 4. 更新（refresh）の保存先解決（TDD）

- [x] 4.1 `refreshNovel` が対象小説の物理位置（親ディレクトリ）を保存先として `startDownload` を呼ぶことを検証するテストを作成・更新する（既存 `refresh_novel_test.dart` のルート前提アサーションを修正）
- [x] 4.2 サブフォルダ内の小説を更新した際にルート直下へ重複しないことのテストを追加する
- [x] 4.3 テストを実行して失敗を確認する
- [x] 4.4 `refreshNovel` のシグネチャを拡張し、保存先（親ディレクトリ）を受け取って `startDownload(outputPath:)` に渡すよう実装する
- [x] 4.5 呼び出し元 `file_browser_panel.dart` を修正し、`dir.path` の親ディレクトリを `refreshNovel` に渡す

## 5. 結合確認

- [x] 5.1 サブフォルダへダウンロード→ファイルブラウザで小説として認識→更新が同じ場所へ上書きされる、までの一連を確認する（手動またはウィジェット/結合テスト）
  - 列挙の小説フォルダ除外・ネスト枝刈り、ダイアログの outputPath 受け渡し、refresh の物理親解決、classifier の leaf 名認識（既存）を単体/ウィジェットテスト群で網羅

## 6. 最終確認

- [x] 6.1 code-reviewスキルを使用してコードレビューを実施
- [x] 6.2 codexスキルを使用して現在開発中のコードレビューを実施
- [x] 6.3 `fvm flutter analyze`でリントを実行
- [x] 6.4 `fvm flutter test`でテストを実行
