## Context

動機と再現手順は proposal.md「Why」を参照。ここでは実装を決めるうえで効く現状だけを挙げる。

- **`directoryContentsProvider` はブラウザ専用に重い。** `file_browser_providers.dart:64` は `.txt` の列挙と並べ替えに加えて、サブディレクトリの列挙、登録済み小説名からタイトルへの解決、そして `tts_audio.db` が存在すればフォルダ別データベースを開いて TTS 状態を読む。読書コンテキストに要るのは最初のひとつだけである。
- **フォルダ別DBハンドルはブラウザの現在地に紐づいて解放される。** `CurrentDirectoryNotifier.setDirectory`（同 `:33`）が、離れたフォルダのハンドルを `perFolderDbRegistry` へ返す。読書側が別フォルダのハンドルを開くと、この解放の網にかからない。
- **小説フォルダの解決規則は既にある。** `resolveNovelFolderPath`（`lib/shared/utils/novel_id_resolver.dart`）。直前の変更で更新対象の解決にも使っており、`reading-progress` やフォルダ別DBと同じキーになる。
- **`selectedNovelTitleProvider` の本番利用者は1つだけ。** `selected_file_progress_title_provider.dart:15` のみ。テストからは複数参照されている。
- **一覧の無効化は5箇所に散っている。** `home_screen.dart:343`、`refresh_progress_dialog.dart:85`、`download_dialog.dart:590`、`tts_controls_bar.dart:204,325,378`。

## Goals / Non-Goals

**Goals:**

- 「いま読んでいるもの」を名前のある1つの導出値にし、タイトルと話送りの双方がそれだけを見る形にする。
- 読書側の話一覧が、ファイルブラウザ用の一覧と同じ並びでありながら、フォルダ別データベースを開かないようにする。
- 一覧の無効化漏れが起きにくい形にする。

**Non-Goals:**

- `directoryContentsProvider` の役割とファイルブラウザのタイル表示の変更。
- **移動が選択を解除するのをやめること（D7）。** フォルダの移動・改名に対するパス追従は本変更の対象だが、移動が選択の寿命に及ぼす影響は変えない。
- `reading-progress` の自動オープンの変更。既読の小説へ入ると表示中のエピソードが差し替わるため、選択の寿命を変える際にはこの意味論も決め直す必要があるが、それは D7 の言う後続の変更で扱う。
- TTS 状態を読書コンテキストへ持ち込むこと。

## Decisions

### D1: 読書コンテキストを `lib/features/reading_context/` に置く

`readingNovelFolderProvider`（読書中の小説フォルダの絶対パス）と `readingEpisodesProvider`（その話一覧）を新設する。

- **なぜ独立した feature か:** capability 名と対応し、`novel_refresh` や `episode_navigation` と同じ粒度になる。`file_browser` に置くと、ブラウザから切り離すための仕組みがブラウザの中にある、という読みにくさが残る。

### D2: 「作品」と「話が並んでいる場所」は別の問いとして解く

読書コンテキストは2つの値を持つ。

- **話一覧のフォルダ**（`readingEpisodeFolderProvider`）= `p.dirname(表示中ファイル)`。隣り合う話と `(N/M)` はここから出る。
- **作品のフォルダ**（`readingNovelFolderProvider`）= 最も近い登録済み小説フォルダ、無ければ話一覧のフォルダ。AppBar が名乗る作品名はここから出る。

- **なぜ分けるか:** 小説フォルダは自身の中に整理用サブフォルダを持てる（ファイルブラウザの「新規フォルダ」で作れる）。`narou_x/分冊/001.txt` を開いたとき、作品は `narou_x` だが、隣にいる話は `分冊` の中の並びである。両方を「最も近い登録済み小説フォルダ」で解くと、話一覧に自分が含まれず、話送りが無言で死に `(N/M)` も消える — 本変更が直そうとしている症状そのものを、別の入口から作ってしまう。
- **なぜ作品側にフォールバックが要るか:** `app-title-display` は「メタデータが未登録のフォルダではフォルダ名を表示する」を既に要求しており、手動で置いたテキストも読める。`resolveNovelFolderPath` は登録外で `null` を返すため、そこで打ち切ると既存挙動を落とす。
- **ライブラリルートは例外:** 作品のフォルダがライブラリルート自身に解決された場合、名乗るべき作品は無い。ライブラリフォルダは作品ではないので、タイトルは `NovelViewer` に倒す。
- 直前の変更の `refreshTargetProvider` は同じ解決を使うが、フォールバックは持たない。更新できる小説は登録済みに限るためで、意図的な差である。

### D3: 話一覧は、フォルダを watch する単一のプロバイダ（family にしない）

`FutureProvider<List<FileEntry>>` が `readingEpisodeFolderProvider` を watch し、`listTextFiles` + `sortByNumericPrefix` だけを行う。サブディレクトリも TTS 状態も読まない。

- **なぜ `directoryContentsProvider` を共用しないか:** あちらは `tts_audio.db` があればフォルダ別DBハンドルを開く。読書側が別フォルダで同じことをすると、ブラウザの現在地に紐づくハンドル解放（Context 参照）の外側にハンドルが1つ増え、Windows でのフォルダ削除・移動を妨げうる。`database-connection-interlock` が守ろうとしている性質に、読書側から穴を開けることになる。
- **なぜ family にしないか:** 当初はフォルダパスをキーにした family にした。これは2つの点で悪かった。非 autoDispose の family はコンテナの寿命ぶんキャッシュを抱え、読んだ小説の数だけエントリが積み上がる。そして `autoDispose` を付けても、非 autoDispose のプロバイダが watch している限り解放されない。結果として、小説を離れて戻っても一覧が取り直されず、**アプリの外で追加されたエピソードが永久に現れない**という、直そうとしていた症状と同じ形の不具合になった。
- **なぜ単一プロバイダで十分か:** watch しているのはフォルダのパス文字列ひとつである。同じフォルダの中でエピソードを移動しても値が変わらないので再計算されず、ページ送りに一覧取得のコストは乗らない。フォルダが変われば再計算される — それがまさに望む「戻ってきたら取り直す」挙動である。
- 並び順は `sortByNumericPrefix` をそのまま使う。ブラウザと別の実装にすると、一覧の順と話送りの順がずれうる。

### D4: 一覧の無効化は共有ヘルパーに集約する

`directoryContentsProvider` を無効化している5箇所を、両方の一覧を無効化する1つのヘルパー呼び出しに置き換える。

- **なぜ:** 片方だけ無効化する箇所が1つでも残ると、更新で増えたエピソードが話送りに現れない、という再発の形になる。呼び出し側が2つのプロバイダを覚えている限り、次に無効化を足す人は片方を忘れる。
- ヘルパーは読書コンテキスト側に置き、ブラウザ側の一覧も一緒に無効化する。

### D5: `selectedNovelTitleProvider` は退役させる

本番利用者はタイトル組み立ての1箇所だけであり、その導出元が読書コンテキストへ移ると、同じ責務が2箇所に残る。spec からも REMOVED として落とす。

- テストは新しい導出のテストへ移す。`selected_novel_title_provider_test.dart` のうち、フォルダ名フォールバックと入れ子解決のケースは新プロバイダ側で引き継ぐ。

### D6: 何も開いていなければタイトルは「NovelViewer」

ブラウザが小説フォルダにいても、表示中のエピソードが無ければ「NovelViewer」になる。これは既存挙動からの変更である。

- **受け入れる理由:** 読者がその状態を目にするのは Drawer を開いている最中であり、Drawer は AppBar を覆っている。エピソードを選べば Drawer は閉じ、タイトルは作品名になる。実際に見える瞬間がほぼ無い一方で、「タイトルは表示中のエピソードについて答える」という規則を1つに保てる。
- 例外を作ると、ブラウザの現在地への依存が「ファイル未選択のときだけ」という形で残り、今回取り除こうとしている結合が半分生き残る。

### D7: 移動が選択を解除するのは、いまは残す

フォルダタップと親フォルダへの移動の `selectedFileProvider.clear()` は残す。移動・改名のパス追従だけを入れ、付け替えは `selectFile` ではなく、ファイルを開く要求を数えない専用の操作で行う。

- **なぜ外さないか:** 外すと、表示中のエピソードに対して働きながら「現在の小説フォルダ」をブラウザの現在地から解決している機能が一斉に食い違う。TTS の生成・削除・書き出し（`tts_controls_bar.dart`）、辞書登録（`text_content_renderer.dart`）、ブックマーク（`bookmark_providers.dart` の `currentNovelFolderPathProvider` とその利用者）、LLM 解析と要約履歴、検索。A の話を読みながら B へ移ってブックマークを押すと B のブックマークが書き換わり、TTS の削除は B の音声を消す。
- **それだけではない:** `setDirectory` は離れたフォルダのフォルダ別DBハンドルを解放する。選択が残ると、TTS 生成中に移動したときに実行中のコントローラが握るハンドルが背後で閉じられる。いまは選択解除が停止を引いている。
- **さらに:** 既読の小説へ入ると自動オープンが表示中のエピソードを差し替える。「入っても読書は続く」を本当に成立させるには、自動オープンの意味論も決め直す必要がある。
- **結論:** 選択の寿命を変えるのは、消費側の移行・ハンドル解放の規則・自動オープンの意味論を一式で扱う独立した変更に値する。本変更はその土台を据えるところまでとする。その代わり、本変更は単体ではユーザーに見える挙動を変えない（`clear()` がある限り「選択がある ⇒ ブラウザはそのフォルダ」が成り立つため）。
- **付け替えを `selectFile` で行わない理由:** `selectFile` は `fileOpenRequestProvider` を加算し、シェルはそれを見て Drawer を閉じる。フォルダ名を変えただけで Drawer が閉じるのは、何も開いていないのに開いたことにしているからである。

## Risks / Trade-offs

- **[ディレクトリ一覧の I/O が1回増える]** → 話一覧のフォルダごとに1回で、同じフォルダの中でページを送る限り再計算されない（D3）。選択が解除される現状では、ブラウザは常に同じフォルダにいるため、同じ内容を二重に読むことになる。読み取りは軽く、TTS もフォルダ別DBも伴わない。
- **[無効化漏れの再発]** → D4 のヘルパーで呼び出し側から選択肢を奪う。テストで、更新完了後に話一覧が取り直されることを確認する。
- **[起動時の復元との順序]** → 復元は小説フォルダへ移動してからエピソードを選ぶ。話一覧は選択後に解決されるため、順序の前提は増えない。一覧の取得中は話送りが一時的に `null` を返すが、これは既存の「選択ファイルが一覧に無い過渡状態」と同じ扱いである。
- **[表示中にエピソードが削除された場合]** → 一覧から消えるため話送りは `null` になる。既存の過渡状態と同じ扱いで、spec にもそう書いてある。
- **[`episode-boundary-prompt` への波及]** → 境界の検出は隣接ファイルの有無に従うため、隣接が正しく解決されれば境界プロンプトも正しく出る。プロンプト側の仕様は変えない。なお「ブラウザが別の場所にいると境界プロンプトが出ない」という状況は、選択が解除される限り起こらない（D7）。後続で選択の寿命が変わった日に、この経路も一緒に正しくなる。
