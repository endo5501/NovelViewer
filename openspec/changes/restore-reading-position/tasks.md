## 1. テストファーストの準備と本文座標

- [x] 1.1 test-driven-developmentスキルを読み、各実装で失敗するテストを先に実行する手順を確認する。スキルが見つからなければ代替配置を検索し、不在を報告する。
- [x] 1.2 ルビ、改行、非BMP文字、結合文字、範囲外の本文オフセットと表示座標変換の失敗テストを追加する。
- [x] 1.3 パース済み本文の共通座標、表示境界への補正、横書き・縦書きとの変換を実装して1.2を通す。

## 2. 履歴DBとRepository

- [x] 2.1 v9履歴の保持、新規作成とのスキーマ一致、既存移行経路を確認するv10マイグレーションの失敗テストを追加する。
- [x] 2.2 body_offsetとbody_hashの追加、およびReadingProgressのシリアライズ拡張を実装して2.1を通す。
- [x] 2.3 同じ話の位置保持、別話のリセット、位置とハッシュの一括保存、最新一件取得の並び順・失敗を検証するRepositoryテストを先に追加し、操作を実装する。

## 3. 保存と復元のコーディネーター

- [x] 3.1 2秒以内の定期送信、連続スクロール、未変更時の書き込み抑制、ライフサイクルflushの失敗テストをfake clockで追加する。
- [x] 3.2 世代付きスナップショット、dirty管理、順序付き保存キュー、WARNINGへの縮退を実装して3.1を通す。
- [x] 3.3 復元前の先頭保存抑制、古い結果の棄却、同じ小説内の連続ファイル切替、失敗後のキュー継続を遅延Futureのテストで先に検証し、制御を実装する。
- [x] 3.4 本文ハッシュ不一致・旧履歴・不正位置・空本文のテストを先に追加し、ハッシュのキャッシュと復元位置検証を実装する。

## 4. 本文ビューアとの接続

- [x] 4.1 横書きの長い折り返し行・ルビ・末尾で、保存位置が復元後に可視となるWidgetテストを先に追加する。
- [x] 4.2 横書きの実レイアウトからの位置取得と一度だけの復元を実装して4.1を通す。
- [x] 4.3 縦書きの禁則処理・ルビ・空行・非BMP文字を含むページ位置取得と復元のWidgetテストを先に追加し、接続を実装する。
- [x] 4.4 フォント・文字サイズ・列間隔・幅・縦横書きの反復変更でも同じ論理アンカーを保つテストを先に追加し、再レイアウト時の保持を実装する。
- [x] 4.5 しおり・検索・次話冒頭・前話末尾・手動操作の優先とTTS追従の非干渉をテストで先に固定し、復元要求の取り消しと消費を実装する。

## 5. 起動とライブラリ操作

- [x] 5.1 最新小説の起動復元、ネスト・移動後のフォルダ解決、履歴なし・対象不在・読込失敗・起動中の手動操作をテストで先に固定する。
- [x] 5.2 起動時ワンショット復元と、フォルダ再入場・通常選択での位置復元を既存Providerへ接続し、5.1を通す。
- [x] 5.3 保存中の削除による履歴復活防止、フォルダ移動とDB解放順序をテストで先に固定し、保存コーディネーターと連携する。
- [x] 5.4 話選択の履歴保存・バッジ更新を維持しつつ位置だけの保存で不要な一覧更新が発生しないことをテストで確認する。

## 6. 最終確認

- [x] 6.1 code-reviewスキルを使用してコードレビューを実施
- [x] 6.2 `fvm flutter analyze`でリントを実行
- [x] 6.3 `fvm flutter test`でテストを実行
- [x] 6.4 macOSで長い話の再起動復元、別小説からの再開、表示変更、しおり・検索・話移動を実機確認する。
- [x] 6.5 Windowsで同じ読書再開フローと通常終了時の保存を実機確認する。環境がない場合は未検証として明記し、完了扱いにしない。
