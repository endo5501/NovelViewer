## 1. 準備・設計確認

- [x] 1.1 `pubspec.yaml` の `version:` 行形式（`X.Y.Z+N`）を確認し、抽出・書き換えに使う正規表現を確定する
- [x] 1.2 スクリプトの仕様（検証項目・順序・終了コード・メッセージ）を spec のシナリオと突き合わせて確定する

## 2. CI 保険（release.yml）— テストファースト

- [x] 2.1 タグ==pubspec 検証ロジックの単体テストを作成する（一致で成功・不一致で非ゼロ終了・ビルドメタ `+N` を無視することを検証）。失敗を確認してコミット
- [x] 2.2 `release.yml` の checkout 直後（ビルド系ステップより前）に検証ステップを追加し、`scripts/verify_release_version.sh` でタグと `pubspec.yaml` の version（`+` 以前）を比較、不一致なら `exit 1` する
- [x] 2.3 一致ケースで続行・不一致ケースで失敗することをローカルで確認

## 3. release.sh（bash）— テストファースト

- [ ] 3.1 一時 git リポジトリ上で各検証シナリオ（形式不正 / dirty / main 以外 / タグ既存 / 後退）が非ゼロ終了し変更を加えないことを検証するテストを作成。失敗を確認してコミット
- [ ] 3.2 ビルド番号インクリメント（`1.1.0+3` → `1.2.0+4`）のテストを作成。失敗を確認してコミット
- [ ] 3.3 引数 `X.Y.Z` 受け取り・事前検証（形式 / clean / main / タグ未使用 / 後退でない）を実装
- [ ] 3.4 `pubspec.yaml` の version を `X.Y.Z+(N+1)` に書き換え → commit → `git tag vX.Y.Z` → `git push`（main + タグ）を実装
- [ ] 3.5 引数なし実行で usage を表示して非ゼロ終了することを実装
- [ ] 3.6 全テストをパスさせる

## 4. release.ps1（PowerShell）— テストファースト

- [ ] 4.1 release.sh と同一の検証シナリオを PowerShell 向け（Pester 等）テストとして作成。失敗を確認してコミット
- [ ] 4.2 release.sh とロジック・メッセージ・終了コードを揃えて実装
- [ ] 4.3 全テストをパスさせ、`.sh` と `.ps1` の挙動が一致することを確認

## 5. ドキュメント

- [ ] 5.1 `.claude/CLAUDE.md` の開発コマンドに release スクリプトの使い方（`scripts/release.ps1 <X.Y.Z>` / `scripts/release.sh <X.Y.Z>`）を追記
- [ ] 5.2 `README.md` の「リリース」セクションを更新し、手打ち `git tag` ではなく release スクリプト経由を推奨手順として記載（pubspec 更新忘れ防止の二段防御に触れる）
- [ ] 5.3 `README_en.md` / `README_zh.md` の対応セクションも同様に更新
