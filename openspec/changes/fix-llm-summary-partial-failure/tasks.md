## 1. 構造化出力（クライアント層）

- [x] 1.1 応答スキーマを表す値オブジェクト（単一文字列フィールド名を保持）を定義するテストを作成し、失敗を確認する
- [x] 1.2 `LlmClient.generate` にスキーマの任意引数を追加する（既定はスキーマなし＝現行挙動）
- [x] 1.3 `OllamaClient.generate` がスキーマ指定時に `format` へJSONスキーマ（`{"type":"object","properties":{"<key>":{"type":"string"}},"required":["<key>"]}`）を送ること、スキーマ未指定時は `format` を送らないことのテストを作成し、失敗を確認する
- [x] 1.4 `releaseResources` が `format` を送らないこと、および `format` に言及する400が再送なしで伝播すること（`think` フォールバックが誤発動しないこと）のテストを作成し、失敗を確認する
- [x] 1.5 `OllamaClient` に `format` 付与を実装し、1.3/1.4 のテストを通す
- [x] 1.6 `OpenAiCompatibleClient` がスキーマ引数を受け取って無視し、リクエストボディが不変であることのテストを作成し、実装して通す

## 2. パイプラインのスキーマ要求とリトライ

- [x] 2.1 Stage-1 が `facts`、Stage-2 が `summary` のスキーマを指定して `generate` を呼ぶことのテストを作成し、失敗を確認する
- [x] 2.2 `LlmSummaryPipeline` の各 `generate` 呼び出しにスキーマを渡す実装を行い、2.1 を通す
- [x] 2.3 Stage-1/Stage-2 とも、生成要求の例外およびパース拒否に対して同一リクエストで1回だけ再試行すること、成功時・生テキストフォールバック時は再試行しないこと、再試行も失敗したら伝播することのテストを作成し、失敗を確認する
- [x] 2.4 単一リトライを実装し、2.3 を通す（プロンプト・スキーマ・パラメータを変更せず、遅延も挟まない）

## 3. パース結果の成否伝達とキャッシュ書き込みゲート

- [x] 3.1 `_parseJsonResponse` 相当の戻り値が「構造化デコード成功」か「生テキストフォールバック」かを区別できることのテストを作成し、失敗を確認する（既存の例外・ログ・フォールバック値そのものの挙動は不変であることも併せて検証）
- [x] 3.2 パース結果に成否を伝える形へ変更し、3.1 を通す
- [x] 3.3 生テキストフォールバック由来の facts、および trim 後に空の facts が `fact_cache` に書き込まれないこと、構造化デコード成功かつ非空の場合のみ upsert されることのテストを作成し、失敗を確認する
- [x] 3.4 `LlmSummaryService` の書き込みゲートを実装し、3.3 を通す

## 4. ファイル単位の失敗継続と明示的失敗

- [x] 4.1 Stage-1 で1ファイルが（リトライ後も）失敗した際に、残りのファイルの抽出が継続され、成功分がキャッシュされることのテストを作成し、失敗を確認する
- [x] 4.2 失敗が1件以上ある場合、Stage-2 の `generate` が呼ばれず、`word_summaries` が書かれず、失敗件数を伴う失敗として終了することのテストを作成し、失敗を確認する
- [x] 4.3 集約後の facts が空の場合に Stage-2 を呼ばず失敗させること、および全ファイル成功時は従来どおり要約生成・スナップショット保存が行われることのテストを作成し、失敗を確認する
- [x] 4.4 失敗の記録・継続・失敗件数の伝達・空 facts ガードを実装し、4.1〜4.3 を通す
- [x] 4.5 部分失敗後の再実行が、成功済みファイルをキャッシュから供給し失敗ファイルのみ再抽出することのテストを作成し、通す

## 5. 再解析時の無効化スコープ

- [x] 5.1 `FactCacheRepository` の強制無効化が基準タイムスタンプを受け取り、`updated_at` がそれより新しい行を対象外とすること、同時刻の行は無効化対象に含めること、基準未指定なら全行を無効化することのテストを作成し、失敗を確認する
- [x] 5.2 `invalidateWord` の条件付き化を実装し、5.1 を通す
- [x] 5.3 再解析時に既存スナップショットの `updated_at` を基準として無効化することのテストを作成し、失敗を確認する
- [x] 5.4 `LlmSummaryService` の再解析経路に基準タイムスタンプを渡す実装を行い、5.3 を通す
- [x] 5.5 再解析が失敗した後の再実行が、直前の試行で抽出済みのファイルを再抽出しないことのテストを作成し、通す

## 6. UI通知

- [x] 6.1 失敗件数を含むスナックバー文言、および facts 皆無時の文言を `AppLocalizations` に追加する（ja / en / zh）
- [x] 6.2 部分抽出失敗時に失敗件数と原因を含むスナックバーが表示され、facts 皆無時は用語名を含む文言、成功時は既存の成功文言、分類できない失敗は既存の汎用失敗文言が使われることのテストを作成し、失敗を確認する
- [x] 6.3 `DefaultAnalysisRunner` の通知分岐を実装し、6.2 を通す

## 7. 最終確認

- [ ] 7.1 code-reviewスキルを使用してコードレビューを実施
- [ ] 7.2 codexスキルを使用して現在開発中のコードレビューを実施
- [x] 7.3 `fvm flutter analyze`でリントを実行 → No issues found
- [x] 7.4 `fvm flutter test`でテストを実行 → 2545 passed, 1 skipped
